# 项目周报：个性化讲义生成模块

**汇报日期：**2026 年 9 月 17 日  
**项目阶段：**技术设计  
**团队规模：**3 人  
**本周交付：**数据结构、资料处理与讲义生成流程、模块接口及成员分工文档。系统编码、数据库部署和效果测试尚未开展。

## 一、本周工作内容

本周围绕个性化讲义生成模块完成技术方案整理。系统以学生信息、知识点掌握记录和课程材料为输入，通过课程内容检索与大模型生成，输出包含学习目标、知识讲解、示例及引用来源的个性化讲义。

本阶段技术方案以文字资料处理为主，采用 PostgreSQL 管理业务数据，使用 pgvector 承载文本向量检索，预留课程页面和图片信息，支持后续接入视觉解析。当前工作范围包括学生状态管理、课程资料处理、讲义生成、结果展示和人工审核。

## 二、系统架构与技术实现设计

### 2.1 模块组成

| 模块 | 职责 | 输入与输出 |
| --- | --- | --- |
| 学生状态管理 | 维护学生信息、学习特点和知识点掌握记录 | 学生标识、课程标识 → 相关知识状态及证据 |
| 课程资料处理 | 提取正文、切分片段、关联知识点并保存来源 | 课程文件 → 带章节和页码的资料片段 |
| 课程证据检索 | 按课程范围筛选资料，执行向量相似度检索 | 教学目标、目标知识点 → 相关证据片段 |
| 个性化讲义生成 | 组织教学内容、调用大模型并检查输出 | 学生状态、课程证据、教学约束 → 讲义及引用 |
| 结果展示与审核 | 展示讲义、来源和调整依据，保存修改 | 生成结果 → 教师审核记录及讲义版本 |

整体数据流程如下：

```text
课程材料 → 正文提取与分块 → 向量化 → PostgreSQL / pgvector
                                              ↓
学生及知识状态 → 教学内容规划 → 课程证据检索
                                              ↓
                         模型输入组装 → 讲义生成
                                              ↓
                         输出检查 → 展示与人工审核 → 结果保存
```

### 2.2 PostgreSQL 数据结构

业务数据以关系表存储，变化较多的补充属性使用 `jsonb`。原始 PDF、课件与图片存放在文件目录，数据库记录文件位置、内容版本及解析结果。

| 表或关系 | 核心字段 | 用途 |
| --- | --- | --- |
| `students` | `student_id`、`profile_json`、`updated_at` | 保存学生标识、明确记录的学习特点与偏好 |
| `courses` / `enrollments` | `course_id`、课程信息；学生与课程关联 | 确定学生所属课程及查询范围 |
| `knowledge_points` | `knowledge_id`、`course_id`、名称、定义 | 统一知识点标识 |
| `knowledge_prerequisites` | 当前知识点 ID、先修知识点 ID | 记录讲解所需的先修关系 |
| `learning_evidence` | `evidence_id`、学生 ID、知识点 ID、来源、观察结果、时间 | 保存教师评价、测验记录等依据 |
| `student_knowledge_state` | 学生 ID、知识点 ID、掌握状态、更新时间 | 表示学生当前的知识点掌握情况 |
| 状态—证据关联 | 学生 ID、知识点 ID、证据 ID | 追溯掌握状态的判断依据 |
| `materials` | `material_id`、课程 ID、文件位置、版本、校验值 | 管理原始课程资料 |
| `material_chunks` | `chunk_id`、资料 ID、正文、页码、类型、解析状态、图片位置 | 保存可检索和引用的内容单元 |
| 片段—知识点关联 | 片段 ID、知识点 ID | 支持一个片段对应多个知识点 |
| `chunk_embeddings` | 片段 ID、模型标识、内容版本、向量 | 存储文本向量并支持语义检索 |
| `generation_runs` | 任务 ID、学生状态快照、目标、证据 ID、模型与提示版本、讲义、运行状态 | 记录生成输入与结果 |
| `lecture_reviews` | 任务 ID、审核者、修改内容、审核状态、时间 | 保存人工审核与讲义修订 |

数据库约束与状态管理：

- 学生、课程、知识点、资料和生成记录使用稳定主键，关联关系通过外键维护。
- 学生与知识点组合在当前状态表中唯一；历史判断依据保存在证据表中。
- 掌握状态区分未知、需要补充、部分掌握和已有掌握证据。缺少记录按未知处理。
- 生成讲义时保存学生状态快照、资料版本和检索片段，保证历史结果可追溯。
- 讲义生成操作不直接更新知识掌握状态；状态更新需要新的学习证据。

### 2.3 课程材料处理与检索

资料处理分为离线入库与生成时检索两个阶段。

**离线入库流程：**

1. 登记课程资料，记录所属课程、原文件位置、版本及校验值。
2. 提取正文和标题，保留原始页码；无法提取正文的扫描页进入 OCR 或人工检查流程。
3. 按标题和段落切分内容。分块长度与重叠量作为配置项，避免截断完整定义、公式解释和例子。
4. 建立片段与知识点的关联，保存图表所在页及图片位置。
5. 调用 embedding 模型生成片段向量，将向量与模型标识、内容版本一同保存。

**生成时检索流程：**

1. 根据教学目标确定目标知识点，并结合先修关系识别需要补充的内容。
2. 限定课程、资料版本与允许使用的章节范围。
3. 分别围绕核心内容和学生知识缺口组织查询。
4. 使用与入库一致的 embedding 模型生成查询向量，在过滤后的资料中执行相似度检索。
5. 去除重复片段，按核心目标与补充内容组织证据，并控制输入模型的总长度。

初始检索使用精确最近邻查询；近似索引在数据规模和延迟测量后再配置。向量维度、返回片段数及分块参数均由统一配置管理，当前不写入未经测试的固定值。

### 2.4 个性化讲义生成

模型输入划分为四部分：教学要求、相关学生状态、课程证据和输出格式。学生状态通过 SQL 精确读取，不通过课程向量库检索其他学生的信息。

| 学生状态 | 对应的内容组织规则 |
| --- | --- |
| 缺少必要的先修知识 | 增加基础回顾和简单示例 |
| 已理解概念但应用不足 | 增加分步推导及步骤解释 |
| 有证据表明存在典型误解 | 增加适用条件、对比和反例 |
| 已有掌握证据 | 减少重复说明，增加迁移应用 |
| 状态未知 | 采用适中深度，提供可选基础补充 |

讲义输出包括教学目标、基础回顾、核心讲解、示例、易错点、小结及来源引用。个性化调整不改变共同教学目标，也不省略必要条件。

证据使用固定的片段 ID 标识，模型引用片段 ID，由程序映射为资料名称和页码。输出后检查结构、引用是否存在、目标是否覆盖及篇幅约束；引用能否支持结论和专业内容是否正确，由人工审核确认。

资料不足时输出缺失项和待补充内容，不生成虚构的课程来源。运行记录保留输入版本、输出、耗时和调用用量；失败任务保留错误信息，不标记为生成成功。

### 2.5 视觉能力接入设计

当前技术方案先实现文字资料流程。资料表保留内容类型、原始页面、图片位置和解析状态，为视觉处理提供统一入口。

视觉能力接入位置如下：

- **入库阶段：**对依赖图表的页面调用视觉语言模型，生成描述或结构化解析结果，与原图、上下文及解析模型版本关联保存。
- **生成阶段：**文字描述不足以表达关键关系时，将相关原图与正文证据共同送入支持图像的模型。

视觉解析结果属于派生数据，需与原始课程内容区分并保留复核状态。页面图像检索不纳入当前文字流程的实现范围。

### 2.6 模块接口设计

以下为模块间的数据契约，不表示接口已经部署。

| 接口 | 请求内容 | 返回内容 |
| --- | --- | --- |
| 学生上下文查询 | 学生 ID、课程 ID、目标知识点 | 相关掌握状态、证据、时间及明确的学习特点 |
| 课程资料检索 | 课程与版本范围、查询内容、目标知识点 | 片段 ID、正文、来源页码、关联知识点及可选图片位置 |
| 讲义生成 | 学生上下文、教学目标、篇幅约束 | 任务 ID、讲义结构、引用、调整说明、待复核项 |
| 讲义审核保存 | 任务 ID、审核者、修改内容、审核状态 | 讲义版本、审核记录与更新时间 |

## 三、成员分工

成员以 A、B、C 标识，职责覆盖数据、生成和交互评价三个部分。

| 成员 | 负责模块 | 具体任务 | 交付内容 |
| --- | --- | --- | --- |
| A | 数据与课程材料 | 数据表与约束实现；学生状态和证据维护；课程文件解析、分块、知识点关联；来源与版本管理 | 数据结构说明、资料导入程序、学生查询接口及标准课程片段 |
| B | 检索与讲义生成 | embedding 接入；受课程范围约束的检索；个性化内容规划；模型输入与输出组织；引用校验和生成记录 | 检索接口、生成服务、提示模板、运行记录及错误处理 |
| C | 交互与评价 | 学生和教学目标选择；讲义及来源展示；审核修改；评价样例整理；问题与教师修改记录 | 演示界面、审核流程、评价样例集和质量检查记录 |

协作衔接：A 向 B 提供学生上下文和资料片段；B 向 C 提供讲义、引用与待复核项；C 将内容错误和使用问题反馈给 A、B，并保存审核结果。

视觉接入时，A 负责页面与解析结果管理，B 负责图文信息接入生成，C 负责代表页面的比较和结果复核。三人共同完成接口联调与讲义样例检查。

## 四、下一阶段任务与检查点

| 成员 | 下一阶段任务 | 检查点 |
| --- | --- | --- |
| A | 建立核心数据表，导入一个章节及学生状态样例 | 可按学生和课程查询知识状态；资料片段能定位到原文件与页码 |
| B | 接入 embedding 与生成模型，贯通检索和文字讲义生成 | 同一主题在不同知识状态下产生可解释的内容差异；引用可定位 |
| C | 完成最小展示与审核入口，整理同主题的不同学生样例 | 能查看讲义、来源与调整说明，保存修改和具体问题 |

共同检查一次完整流程：选择学生与目标 → 查询状态 → 检索资料 → 生成讲义 → 查看来源 → 保存审核结果。检查覆盖内容正确性、目标覆盖、个性化依据、引用有效性和失败状态处理。

## 五、技术依据

- [PostgreSQL JSON 数据类型](https://www.postgresql.org/docs/current/datatype-json.html)：补充属性的结构化存储。
- [pgvector 官方文档](https://github.com/pgvector/pgvector)：向量存储、精确查询和近似索引。
- [PyMuPDF OCR 文档](https://pymupdf.readthedocs.io/en/latest/recipes-ocr.html)：扫描页面的文字提取路径。
- [ColPali 论文](https://arxiv.org/abs/2407.01449)：视觉信息丰富文档的检索方法参考。
